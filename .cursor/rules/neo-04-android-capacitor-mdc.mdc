---
description: Android / Capacitor subtree — follow android AGENTS and docs (N.E.O.)
globs: android/**/*
alwaysApply: false
---

# Android subtree

## Source of truth
- Read `android/AGENTS.md`, `android/BRANCH_GUARDRAILS.md`, and `android/docs/*` before editing.

## Consistency with web
- If the task is "align auth/data with web," prefer the **same Supabase** architecture as root `docs/TARGET_SUPABASE_ARCHITECTURE.md` unless `android/` docs explicitly defer.

## UI
- Same **Robot2D vs Robot3D** and **no layout churn** policy as root unless an android prompt explicitly allows it.
